server:
  port: 9004

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 1000000

ribbon:
  ConnectTimeout: 60000
  ReadTimeout: 60000
  eager-load:
    enabled: true

zuul:
  routes:
    adel: /adel/**
